# Data

Versioned game data recipes and resources.

- `levels/` — `LevelRecipe` JSON (id, seed, dimensions, piece set, blockers, objectives, move limit, chapter, generator version).
- `chapters/` — chapter / map definitions.
- `localization/` — English `.po`/`.csv` keys.

No generated 10,000 scenes live here; everything is data.