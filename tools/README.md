# Tools

Offline tooling kept inside the project tree.

- `levelgen/` — deterministic recipe generator (Step 22).
- `validation/` — solver / release validator (Step 23).
- `build/` — local headless Android debug build helpers (Step 04 onward).

All tooling must run offline and never require network access.