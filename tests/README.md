# Tests

- `unit/` — domain unit tests (no Godot scene dependency).
- `integration/` — full session tests.
- `smoke/` — boot + scene-load smoke tests.

Test framework is selected in Step 03. Until then this folder is reserved.